{% extends 'mainBundle:Main:layout.html.twig' %}

{% block body -%}
    <h1>Salle edit</h1>

    {{ form(edit_form) }}
    
    {% if salle.image is not null %}
		<img src="{{ salle.image.getWebPath }}" />
	{% endif %}

        <ul class="record_actions">
    <li>
        <a href="{{ path('reservationbundle_salles') }}">
            Retour à la liste
        </a>
    </li>
</ul>
{% endblock %}
